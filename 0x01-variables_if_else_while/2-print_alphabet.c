#include <stdio.h>

/**
 * main - prints out the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char y = 'a';

        while (y < 'z')
 {
	putchar(y);
	y++	}

   putchar('\n');
	return (0);
}
